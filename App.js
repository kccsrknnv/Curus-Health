import React from 'react';
import BasicFlatList from './UI.js';  
import { Alert, AppRegistry, Button, StyleSheet, View } from 'react-native';  
const App = () => {
   return (
      <BasicFlatList />
   )
}
export default App